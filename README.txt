geography-30.github.io (root) — Static site (No JS)
---------------------------------------------------
- Upload all files to the repository root.
- Keep `.nojekyll` in the root to disable Jekyll processing.
- Add new games by duplicating `game-template.html` and linking it from index.html.
- Replace <iframe src="about:blank"> with your actual game URL or local file path.